export interface CelebrityData {
    id: number;
    first: string;
    last: string;
    dob: string; // Date of Birth, formatted as "YYYY-MM-DD"
    gender: string;
    email: string;
    picture: string; // URL to the character's picture
    country: string;
    description: string;
}